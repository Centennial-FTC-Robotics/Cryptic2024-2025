package org.firstinspires.ftc.teamcode.test;

import com.acmerobotics.dashboard.FtcDashboard;
import com.qualcomm.robotcore.eventloop.opmode.Autonomous;
import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;

import org.Cryptic.Robot;
import org.Cryptic.Subsystems.DrivetrainNoRR;

@Autonomous
public class SpecimenTest extends LinearOpMode {
    @Override

    public void runOpMode() throws InterruptedException {
        /*
        FtcDashboard dashboard = FtcDashboard.getInstance();
        Robot robot = new Robot();
        robot.initialize(this);

        waitForStart();

        robot.dtNoRR.driveDistance(3);

        robot.outtake.specimenSequence = 2;
        robot.outtake.currentActionSequence = "Outtake Specimen";
        robot.outtake.specimenState = 2;

        robot.outtake.specimenScoreAuto();
        */
    }
}
